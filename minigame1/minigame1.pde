float x, y;
PImage boat;
void setup() {
  size(1280, 720);
  colorMode(HSB, 360, 100, 100);
  boat = loadImage
}

void draw() {
  noStroke();
  fill(190, 80, 99);
  rect(0, 0, width, height);
  fill(210, 90, 99);
  rect(0, height/4, width, height);
  
  
}